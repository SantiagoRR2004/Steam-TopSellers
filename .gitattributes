# Auto detect text files and perform LF normalization
* text=auto

# Use LFS for JSONL files
*.jsonl filter=lfs diff=lfs merge=lfs -text

# Detect CSV and JSON files
*.csv linguist-detectable=true
*.json linguist-detectable=true